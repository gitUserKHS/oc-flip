# CLAUDE.md — OC-flip 프로젝트 인수인계 브리프

> 이 파일을 레포 루트에 두세요. Claude Code 세션은 여기서 시작합니다.
> 대화 언어: 한국어. 코드 주석·논문: 영어.

## 프로젝트 한 줄

SIMP 위상최적화의 감쇠 OC 갱신을 로그좌표 동역학계로 분석: 국소 승수 **μ = 1 − η·s**, flip 문턱 **η\* = 2/s_max**, 표준 (p,η)=(3,½)의 한계성(marginality)을 top88에서 연산자 수준으로 실측. 목표: SMO 정규 논문 1편 (+ 후속 통계역학 논문 씨앗).

## 현재 상태 (2026-08-16)

- 사이클 1–5 완료, 관문 2개(선행권·강건성) **통과**. 논문 골격 확정(`notes/paper_skeleton.md`) + 골격 패치 v3(`notes/cycle5_note.md` §6).
- 사이클 5(마감 실험)에서 백로그 2·3·4 완료: 정확 접공간 사영, 음수 가지 종결 규명(대칭 파괴), p-스윕 본실험(**"p=5 이탈" 반증** — 포화 법칙 전 구간 유효, 문턱=고정점 성질), rmin detuning, V5 온셋 브래킷.
- **paper/main.tex = v2 (공학 프레이밍, 2026-08-17)**: 제목 "Damping without tuning: ...", 기여 4개, §6 컨트롤러(설계·데모·실패율·Anderson), 음수 가지는 부록 A. 물리 프레이밍 v1은 `paper/main_v1_spectral.tex` 아카이브(통계역학 2편 출발점). 빌드: `latexmk -pdf`(bibtex가 outdir에서 refs.bib를 못 찾으면 복사 — 한글 경로 이슈).
- **공학 트랙 (Opus 5 외부 리뷰) — 사이클 A·B·C 전부 완료**: A(관문: ŝ 이중 국면+리처드슨), B(AIMD 최소 구현), C(스윕 실패율+Anderson 조합+MMA 열+논문 v2). 상세 `notes/cycleA_note.md`, `cycleB_note.md`, `cycleC_note.md`.

## 권장 레포 배치

```
oc-flip/
  CLAUDE.md                  # 이 파일
  notes/                     # cycle1_note.md ... cycle5_note.md, gate2_priorart.md,
                             # paper_skeleton.md, review_gpt_response.md
  src/                       # oc_toy.py, top88_instrumented.py, probe_experiment.py,
                             # jacobian_spectrum.py, c4_core.py, c4_run2.py,
                             # c5_core.py, c5_run.py, c5_fig.py
  data/                      # c4_V1..V5.npz, c5_*.npz (psweep.npz는 구버전 — 함정 10)
  figs/                      # fig_cycle1..5.png, fig_cycle2c.png, fig_psweep.png
  paper/                     # main.tex (미작성), refs.bib
```

의존성: numpy, scipy, matplotlib (그 외 없음). 재현 명령 일람: `README.md` §Reproduction.

## 이론 코어 (암기용)

- 순수 OC: x⁺ = clip(x·B^η), B = D̃/λ. 로그좌표 선형화 → **μ = 1 − η·s**, s = −∂log D̃/∂log x (자유집합, 부피 널방향 제외).
- 안정창 η ∈ (0, 2/s); **1-스텝 η = 1/s**(창의 중점); flip 경계 η = 2/s.
- 정정계 직렬 토이: s = p+1 (p=3 → η\* = ½ 정확히). 병렬 토이: s = −(p−1) → bang-bang.
- 이동제한·클리핑·kick은 문턱 불변, 진폭만 결정.
- SAO 사전(Groenwold–Etman 2008): OC-η ≡ 지수 매개변수 y = x^a, **η = 1/(1−a)**. a=−1(역수) ⇔ η=½; a=−p(정확) ⇔ η=1/(p+1). 역수 곡률 = p=3 참곡률의 절반 → μ=−1 → flip 경계. **이 동치의 발견 주장 금지, 귀속 필수.**

## 확정 수치 (전부 p=3, move=0.2, XMIN=1e-3)

| 케이스 | 설정 | s_max | η\*=2/s_max | 검증 |
|---|---|---|---|---|
| R0 | 60×20 MBB sens r1.1 vf.5 | 3.974 | 0.503 | 온셋 (0.5,0.6]; 여유 0.003 |
| V1 | 120×40 MBB sens r1.1 | 3.625 | 0.552 | 브래킷 (0.45,0.55] (경계) |
| V2 | 120×40 MBB sens r2.2 (R0 물리반경 동일) | 3.850 | 0.519 | (0.479,0.559] ✓ |
| V3 | 60×20 캔틸레버 sens r1.1 | 3.486 | 0.574 | (0.534,0.614] ✓ + 문턱하 3자리 |
| V4 | 60×20 MBB sens r1.1 **vf.4** | 2.888 | 0.693 | (0.653,0.733] ✓ + 3자리 |
| V5 | 60×20 MBB **밀도필터** r2.4 | **0.370** | ≈5.41 | flip 미관측(정성) |

- 야생 항등식 μ̂ = 1−ηŝ: 28실행 소수점 3자리 (사이클 2).
- s-스펙트럼 η-불변: η=0.5/0.9 빌드 상위 5값 3자리 동일 (3.974/3.932/3.893/3.784/3.763).
- 모드=진동패턴: |cos| = 0.93 (r1.1, 국소 패치) / 0.96 (r2.4, 광역 2엽).
- 포화 2-주기 자기조직화: η·ŝ = 2 (η 1.0→ŝ 1.996; 1.2→1.667; 1.6→1.250).
- 문턱하 감쇠율 3자리: V3 예측/실측 0.583/0.583, 0.862/0.862, 1.140/1.141; V4 0.886/0.886.
- 필터 역설: 과구동 시 컴플라이언스 r2.4 216→346(+60%), r1.1 195.6→202.6.
- 용량 적응 design(η): r1.1 부분 적응(0.504→…→0.728), r2.4 동결.
- 성능: 스텝 60×20 9ms / 120×40 54ms; V2 아놀디 50 matvec ≈ 3s (전체 J 51s와 s_max 일치).
- **p-스윕 본실험 (사이클 5, 60×20 MBB r1.1, 기준 프로토콜 η₀=0.3)**: 포화율 s_max/(p+1) = 0.961~0.995, **p∈[1.5,5] 전 구간** (p=5: s_max 5.969, 0.995). 행동 브래킷 8/8 예측 포함, 비율 법칙 42프로브 중앙편차 0.001. ~~p=5 진짜 이탈~~ **반증** — 프리뷰 4.330은 다른 고정점(함정 10). `data/c5_psweep_ref.npz` (구 `psweep.npz`는 프로토콜 미기록, 참고용만).
- **자기제한/적응 천장 (사이클 5)**: 적응 프로토콜(η₀=0.5 사다리)은 p>3에서 s_max ∈ [3.26,3.84] < 2/η₀=4 고정점으로 표류; 갈림점 p+1=2/η₀ ⇔ **p=3**. 같은 p=5 문제의 두 고정점(5.97/3.52)이 각자 2/s_max 온셋 (0.300,0.370] / (0.528,0.608]을 따름 — **문턱은 고정점의 성질**. `data/c5_psweep.npz`.
- **rmin detuning (사이클 5)**: p=5 s_max 5.97(r1.1)→2.95(r2.4) −50%, p=3 3.98→3.0~3.4 −15~24% — "회색층 두께" 가설 기각, **감도필터 반경이 flip 가지를 detune (p 클수록 민감)**. V5 밀도필터(0.37)와 연속 스펙트럼. `data/c5_p{5,3}rmin_ref.npz`.
- **음수 가지 (winner-take-all, 사이클 5 마감)**: V3 s_min=−0.04308(정확 사영), μ(0.5)=1.0215 — 킥 후 성장률 4자리 일치(n=76). **정체 = 거울쌍 대칭 파괴** (서포트 상위 20 전부 ±쌍, |v|² 77%가 x<0.05). 종결 = XMIN 흡수 아님(0개), **인접 비대칭 고정점 재정착**(자유집합 352→348), 종결 후 s_min=+0.0375(소멸), 컴플라이언스 180.56→180.52. 대칭해=안장. p=3 MBB s_min=−0.102, p=4 −0.398, p=5 +0.239(소멸). **2/s_max는 "양수 가지의 flip 경계"로 서술할 것.** `data/c5_negbranch.npz`.
- **정확 접공간 사영 (사이클 5)**: J_T=QᵀJQ (w=x|_F sens), 휴리스틱과 top6 차 ≤5.3e-5, 행-널 ‖wᵀJ‖/‖J‖≈2e-4 실측, 원시 J에 μ=0 구속 아티팩트 s=2.000 (6자리) — 논문 §4는 정확 사영으로 서술. `c5_core.tangent_spectrum`.
- **V5 온셋 브래킷 (사이클 5)**: 다중 시드 {5,7,11}×η{1,2,4} 무진동(amp ~3e-5) + 프로브 온셋 **(5.4, 6.0]** ∋ 예측 5.41 — 정성→반정량 승격 (nmid=29 잡음, 3자리 주장 금지).
- **ŝ 이중 국면 (사이클 A)**: 수렴 국면 ŝ ∈ [−0.04, +0.15] (s≈0 크립 가장자리; s_max와 무관), 포화 진동 **η·ŝ = 2.000 (3–4자리) 14/14런** — 사이클 2의 3점 확장. 전이대에서 μ̂ 부호가 게이트. `data/ca_sweep.npz`.
- **리처드슨 최적성 (사이클 A)**: 18/18 스펙트럼에서 수치 argmin_η max|1−ηs| = 2/(a+b) 4자리 일치; a/b ≤ 0.02 (17/18, p5_adapt 0.069), 벌크 중앙값 s ≈ 0.95–1.0 ⟹ **η_opt = η\* = 2/s_max와 1–2% 이내**. R0에서 η_opt = 0.4990 ≈ folklore 0.5. **"marginality = 최적성의 서명"** — 헤드라인 전환. 벌크 감쇠: η 0.5→0.25에 스텝당 0.5→0.75 (100반복 4e17배). `data/ca_richardson.npz`.
- **행널 잔차 정직화 (사이클 A)**: V3 ‖wᵀJ‖/‖w‖ = 2.5e-3 (‖J‖₂ 기준 1.2e-3; 기존 ‖J‖_F 기준 1.9e-4는 후한 정규화). 중심차분 개선 미미(→2.0e-3) → 바닥은 클리핑·이분법 구조. top-6 s는 스킴 간 1.1e-5.
- **AIMD 적응 감쇠 (사이클 B·C, 최종 동결 상수)**: 규칙 = μ̂_w<−0.75 ∧ ŝ_w>0.1 ⟹ η←1.7/ŝ_w (쿨다운 5) / **크립 서명(μ̂_w>0.9 ∧ |ŝ_w|<0.3) 동결** / 그 외 η+=0.01; 워밍업 25, 창 4 — **절대 진폭 상수 0개, 전 실험 공통**. B 데모: 6/6 수렴, η 자동 안착(r1.1 {0.45–0.57}, r2.4 {0.46–0.56}), 과구동 구조(313→217.10), 과감쇠 보너스(194.6), 무해성(+0.1%). `data/cb_aimd.npz`.
- **튜닝 실패율 스윕 (사이클 C, 11그룹×400반복)**: **fixed0.5 = 0/11 무결(리처드슨 최적성의 예측 그대로 — 정직 헤드라인)**; fixed0.7 4/11·fixed1.0 9/11 진동; AIMD(0.5) 1/11(120×40 경계 라이딩, 품질 무해)·AIMD(1.0) 2/11+1(vf0.4_p5 +18%, 사다리·0.7도 죽는 최악 그룹); 사다리 1+2; MMA 참조열 0/11 진동·1 품질(±수 % 양방향). 컨트롤러 가치 = "0.5를 이김"이 아니라 "선택 무의미화". `data/cc_sweep.npz`, `cc_mma.npz`.
- **Anderson 조합 (사이클 C)**: **대칭 실패 패턴** — AA(주기적 type-II, m=4, K=4)는 과구동 완치(217.11, 3e-11)하나 **잘 튜닝된 0.5를 파괴(253.4, +17%; 감쇠 β=.5도 완화만)**. 고정은 1.0에서, AA는 0.5에서 실패, AIMD 단독 무실패, **AA+AIMD 양쪽 최심 수렴(2.0e-6/4.2e-11)** — MD 2·6회가 간섭 공진 차단. `data/cc_anderson.npz`.

## 작업 백로그 v6 — arXiv 로드맵 (외부 리뷰 5단계 플랜; 총 4–5일 각)

0. ~~숫자 감사~~ **완료(2026-08-17)** — 상세 `notes/cycleC_note.md` §6. 부산물: **PR 국소화 실측**(p3 r1.1 PR=10.8/360 → r2.4 47.7), **AIMD(0.5) 최악 +0.7% = 전 방법 최고**(Table 4 최악값 컬럼).
1. ~~p-continuation~~ **완료(2026-08-17)** — 상세 `notes/step1_cont_note.md`. **fast 램프가 캔틸레버에서 fixed 0.5를 깸**(프로젝트 최초 folklore 경성 실패; slow에선 생존 — "램프 vs 정착 경주"), fixed 0.7은 5/6 OSC, AIMD 12/12 수렴. **종점 가족 분리 실측**: fixed0.5 → s_max=4.0(=2/η 천장, 두 스케줄), AIMD(1.0) → 6.0(=p+1). **η(t)가 2/(p(t)+1) 점대점 추적**(0.81/0.80→0.36/0.33; 캔틸레버는 자기 고정점 문턱 0.55 추적). 뉘앙스: r1.1은 천장 고정점이 컴플라이언스 우수(195 vs 198) — 추적≠분지 품질. 표지 그림 후보 `figs/fig_cont.png`(a). 논문 §6.3 신설. `data/cd_cont.npz`.
2. ~~방어 실험 3종~~ **완료(2026-08-17)** — 상세 `notes/step2_defense_note.md`. ① move-limit: **η=1.0에서 8/11 "가면 쓴 진동"**(진폭 통과·μ̂=−1.00·move 바닥) — 기제 확증 + "진폭 정지조건은 속고 μ̂는 안 속는다" 판매 포인트, Table 4 +2행. ② 종점 6개: 비 η_end/(2/s_max^end) = 0.77–1.01 — **자기 고정점 경계 직하 주차**(리처드슨 자리), §6.2 폐루프. ③ 절단: R0 η_opt 0.499→0.444(컷 0.5), 전 컷 12% 이내 — §5.3 정직 3조항+절단 수치 반영. `data/ce_*.npz`.
3. ~~통계~~ **완료(2026-08-17)** — 상세 `notes/step3_stats_note.md`. 231런: **fix0.5도 seed 7에서 1/11 진동(방탄 아님)**, aimd0.5는 2/3시드 0/11 — 0-vs-1 유의성 부인 명시. 시드-강건 구별 = fix1.0 매시드 9/11(최악 +61%), fix0.7 3–6/11 요동, **최악값 격차(컨트롤러 +0.7–1.6% vs 이탈 +15–61%)**. §6.4+캡션 반영. `data/cf_seeds.npz`.
4. ~~본문 재작성 (데이터 무관분)~~ **완료(2026-08-17)** — 초록 재구성(실측 법칙 3개 전면), 기여 4→3, §2 MMA 점근선 차별화("무차원이나 비캘리브레이션 vs 승수 캘리브레이션"), §5.2 period-2 본문 승격+**period-4 미관측 명시**, §4 η-불변=정합성 체크 톤, §3.1 w_e 명시(sens/dens), Fig1 캡션 3패널 정합(유령 D 제거), §8 음수가지·시간척도 2줄. 잔여: 없음(§5.3 정직화·§6 재배치는 2단계·C에서 기완료).
5d. ~~제출 전 위험 2건~~ **완료(2026-08-17)** — 상세 `notes/prearxiv_note.md`. (a) §2에 **estimator-driven relaxation 계보 문단** 신설(Irons–Tuck 1969 / Küttler–Wall 2008 / Barzilai–Borwein 1988, 전부 DOI 확인) + 차별화 2축(지수 vs 가법 이완계수 / 무조건 소비 금지 = 국면 게이트가 사는 것). 식(5) 무인용 리스크 해소. (b) **초록·서론·결론의 "optimal" 톤을 §5.3과 정합**(최악률 공허·벌크가 일함 인정). 초록 280단어, Draft v3. 데이터 가용성 문장 완성형(URL/DOI만 남김).
5e. **engrXiv 제출 준비 완료 (2026-08-17)** — 상세 `notes/submission_note.md`. 저자 **Hyeongseok Kim, Independent Researcher, Uiwang, Republic of Korea**. **서지 전수 검증 완료**: 낡은 인용 1건 교체(Keith et al. → SIAM J. Optim. 35(2):1134–1164, 2025), 12건 권호·DOI 보강, groenwold2008/andreassen2011의 online-vs-print 연도 확인(둘 다 현행 유지). 데이터 가용성은 "기탁 후 후속 버전에 식별자 추가" 문구. **업로드 파일 = `paper/main.pdf` 하나**. 순서: engrXiv 선공개 → Zenodo 기탁 → DOI 넣어 v2 갱신.
5. **Zenodo 아카이브 패키지 (v2용)** — 저자 표기 포함. `paper/main.tex`에서 TODO 전부 제거(저자 블록 삭제, 데이터 가용성은 자기참조형 완성 문장), README를 배포본용으로 재작성, `ZENODO.md`에 붙여넣기용 메타데이터(제목·설명·키워드·체크리스트) 준비, `C:\연구_프로젝트\oc-flip_zenodo.zip` (94파일 6.2MB, 무결성 확인) 생성. **업로드 시 저자(creators)와 라이선스만 선택하면 됨** — Zenodo 필수 항목. 게시 후 DOI를 여기 기록할 것.
   - 이후 저널 투고를 재개한다면: 저자·사사, sn-jnl 변환+플로트 배치, 고전 문헌 쪽수 스팟체크, `notes/review3_note.md`의 리비전 등급 항목. ~~그림 1–3·6 restyle~~ **완료(2026-08-17)**: `src/pub_figs.py` → fig_toy_pub(신규 도식 포함)/fig_identity_pub(설계 대비 포함)/fig_spectra_pub(스펙트럼 형상 히스토그램 — 리처드슨·크립의 시각적 근거; 모드 |cos| 독립 재현 0.91)/fig_robust_pub. 전 그림 serif+STIX 통일.
5c. ~~리뷰 3라운드 16항목(A4·B4·C8)~~ **완료(2026-08-17)** — 상세 `notes/review3_note.md`. 핵심: 서론을 세 법칙+continuation 구조로 재작성(본문 따라잡기), tab:cont에 속도 축 4행 추가로 **fixed 0.3 반례 차단**(진동 0/6이나 중앙 dq +1.38%·최악 +3.2% = 수렴 방법 중 최대), **vf0.4 인과 측정으로 추측 정정**(`data/ch_vf04.npz`: 자연 종점 s_max≈6.0·문턱 0.333이 0.5 배제 — 사실; 단 fixed0.5 자신의 종점은 비고정점 구동 상태 3.7–3.9로 낀 상태), 최악값 시드 짝비교, 캐스케이드 주장 하향("범위 내 미도달"), 초록 286단어.
5b. ~~리뷰 2라운드 11항목~~ **완료(2026-08-17)** — 상세 `notes/review2_note.md`. 신규 발견 2건: (a) **masked oscillation 일반화** — fixed 0.7의 "수렴" 3런이 표준 move에서 μ̂=−1.00 조용한 한계 사이클(실질 실패 7/11), Table 4에 masked 컬럼(기준: amp∈(1e-5,3e-3)∧μ̂≤−0.9; 수치 바닥 감쇠 잔류는 제외); (b) **continuation 동기 방탄화** — cant_fast fix0.5 3/3 재현 + vf0.4 신규 구성 8/8 전패(slow 무용 — 종점 문턱<0.5), fix0.5 12/32·AIMD 0/64. 캐스케이드 직접 테스트로 하한 클리핑 배제(1e-6 바닥에서도 주기-2; §5.2 "bounded update, 기제 미분리"로 정정). `data/cd_seeds.npz`, `src/cg_cascade.py`.
6. (이후) AA 간섭 규명(사이클 D 후보), vf0.4_p5 기제, 120×40 예산 연장, 다중하중·rmin↓, run_all 갱신, 2편 씨앗(`main_v1_spectral.tex`).

## 함정 목록 (돈 주고 산 교훈)

1. **킥 없는 연속실행은 온셋을 못 봄** — x₀에 불안정 모드 씨앗이 없음. 반드시 섭동 킥.
2. **로그-크립**: 저밀도 회색 원소(x~0.005)가 로그노름을 지배(120×40에서 ‖dz‖~0.3–0.55, 600반복에도 서행) → 프로브는 **x-공간 모드정렬**로 (`c4_core.mode_probe`).
3. **고유쌍 진위**: 비정규 행렬이라 의심되면 독립 ε FD matvec으로 잔차 확인 (V1에서 2.7e-4, ε-불변 확인 절차 표준화됨).
4. **복소 모드**: 문턱은 η\*_m = 2Re(s)/|s|² 최솟값 (`eta_star_complex`) — 지금까진 상위 모드 전부 실수.
5. FD 결정성: OC 이분법 tol 1e-10 유지 (λ 잡음이 FD에 새는 것 방지).
6. 큰 |F|는 아놀디 (`arnoldi_smax`, 평균중심 deflation) — 전체 J와 s_max 일치 검증 완료.
7. 밀도필터의 s는 **설계변수 공간** 기준 (xPhys 아님) — 서술 시 명시.
8. 인용 지뢰: Martínez 2005는 문제 수준(위협 아님), Mueller–Burns는 "반복=동역학계" 원조로, G–E 2008은 η 정역학으로 반드시 귀속. 상세 `notes/gate2_priorart.md`. Munro η-곡률 논의는 **표적 검색으로도 미발견(2026-08-16 종결)** — 인용 금지, G–E SAO 계열(2007–2010)로 갈음. Li 인용은 학위논문 아닌 출판본(li2020fixedpoint, MRC 2020)으로.
9. **스펙트럼은 양끝을 다 봐라** — s_max만 보다가 s_min<0(음수 가지)을 외부 리뷰가 먼저 발견. "안정창 (0, 2/s_max)" 같은 전체-안정성 표현 금지, 항상 가지별로 서술.
10. **s_max는 고정점의 성질 — 수렴 프로토콜을 명시하라.** η₀=0.5 수렴은 p>3에서 s_max<2/η₀ 고정점만 도달(관측 선택 편향; "p=5 이탈" 오판의 원인). 포화 법칙 검증은 저-η 기준 프로토콜(η₀=0.3)로. 프로토콜 미기록 데이터(구 psweep.npz)는 재사용 금지.
11. **수렴 깊이가 프로브 신뢰도를 결정** — amp ≥ 1e-4급 얕은 수렴은 잔류 과도가 모드정렬 프로브를 오염(adapted p=4.0 브래킷 예외의 원인). 브래킷 주장 전 amp 확인.
12. **naive η=c/ŝ은 발산한다** — 수렴 국면의 ŝ은 s_max가 아니라 s≈0 크립 가장자리를 읽음(실측 [−0.04, +0.15]; mid 마스크로도 못 구함). 적응 규칙은 반드시 μ̂ 부호로 게이트(AIMD 구조).
13. **μ̂=1−ηŝ 항등식은 추정기 검증이 아니다** — 같은 궤적의 두 추정기라 어느 모드를 읽든 성립(순환). 프레임워크 검증으로만 인용; "ŝ이 무엇을 읽는가"는 사이클 A 스윕이 규명.
14. **AIMD 상수는 서로 정합해야 한다** — (a) MD 직후 승수 1−c가 게이트 −(1−δ) 밖이어야 재발화 없음 (c=1.7 ⟺ δ<0.3); (b) 동결은 절대 진폭이 아니라 **크립 서명(μ̂>0.9∧|ŝ|<0.3)**으로 — freeze_amp=1e-3은 120×40 크립 바닥 위라 실패; (c) 게이트 δ는 약진동 온셋(μ̂≈−0.7~−0.8) 기준 — δ=0.1은 부분 적응 케이스에서 폭주; (d) **α는 단독 성능 vs 조합 강건성 트레이드** — 0.005는 스윕 완벽·AA 조합 파괴, 0.01로 동결. 지배 세트는 없다 — 동결 지점을 정하고 이력을 논문에 공개.
15. **AA는 잘 튜닝된 런을 깨뜨릴 수 있다** — 주기적 type-II AA가 η=0.5 건강한 수축과 간섭(+17%). 그 간섭 진동은 flip 서명이 아닐 수 있어 μ̂ 게이트가 못 볼 수 있음(α=0.005에서 MD 미발화). 조합 주장은 변형·시작점 명시로 한정.
16. **테스트셋 보고 컨트롤러 재튜닝 금지 (외부 리뷰 지시)** — AIMD(0.5)의 1/11 태그를 없애려는 α 감쇠·게이트 추가 = 오버피팅이자 "3회 설계 반복이 전체 튜닝 이력" 신뢰 축 파괴. 대신 boundary-riding을 설명하고 반복수·컴플라이언스로 무해성을 독자가 판단하게 하라.

## 스타일 규약

- 짧은 사이클 + 관문식 진행, 각 사이클 끝에 `notes/cycleN_note.md`.
- 주장 다이어트 준수 (`paper_skeleton.md` §6): 전역 수렴·MMA 진술·발견 주장 금지 항목.
- 수치는 노트에 3자리로 고정 기록; 코드 시드 고정(seed=1, 5).
