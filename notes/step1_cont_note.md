# 1단계 노트 — p-continuation: 움직이는 문턱 실험

**날짜:** 2026-08-17 · **판정: 성공 — 예상 페이로드 2종 + 뉘앙스 1종 확보. 표지 그림 후보 획득.**

재현: `python src/cd_cont.py` (~8분, 36런) → `python src/cd_fig.py`. 데이터 `data/cd_cont.npz`, 그림 `figs/fig_cont.png`. 설계: p 1→5 (0.25씩; fast N=20/예산 600, slow N=40/900), 구성 {MBB r1.1, MBB r2.4, 캔틸레버 r1.1}, 방법 {fixed 0.3/0.5/0.7, 사다리(1.0), AIMD(0.5/1.0)}, 컨트롤러 상수 동결 유지(함정 16).

## 결과

| 그룹 | 핵심 |
|---|---|
| cant fast | **fixed 0.5 진동**(tail 4e-3, +2.9%) — 프로젝트 최초의 folklore 경성 실패. fixed 0.7도 5/6그룹 OSC. **AIMD 12/12 수렴**, aimd1.0이 그룹 최고(182). slow에서는 fixed 0.5 생존 — **실패는 램프 vs 정착의 경주**(고정 파라미터가 알 수 없는 정보). |
| r1.1 (fast·slow) | **종점 가족 분리**: fixed 0.5 → s_max **4.0 = 2/η 천장**(두 스케줄 모두 정확히), AIMD(1.0) → **6.0 = p+1 자연 포화**. §5.4의 두 프로토콜 분리가 인위적 설계 없이 현실 워크플로에서 자발 재현. |
| 추적 | AIMD(1.0)의 η(t)가 2/(p(t)+1)을 점대점 추적: 0.81/0.80 → 0.45/0.44 → 0.39/0.35 → 0.36/0.33. AIMD(0.5)는 초반 상향 탐사(문턱이 아직 위 — 올바른 행동) 후 p≈3에서 하강 곡선에 붙잡혀 동행. **캔틸레버에서는 자기 종점(s=3.7)의 문턱 ≈0.55를 추적** — 공식이 아니라 실제 수렴 중인 고정점을 따름. |
| 뉘앙스 (정직) | r1.1에서는 천장 고정점(s=4)이 컴플라이언스 우수(195 vs 198, +1.5%), 캔틸레버에서는 컨트롤러 종점이 최고 — **추적은 안정성 보장이지 분지 품질 보장이 아님**(§7 재확인). r2.4는 필터 detuning으로 전 방법 ≈224 (문턱 0.6>0.5라 스트레스 없음 — 필터 결과의 예측 그대로). |

## 논문 반영

- §6.3 "A moving threshold: penalization continuation" 신설 (스윕 6.4·Anderson 6.5로 밀림), Fig(fig_cont) 삽입, 초록·기여 3에 한 줄씩.
- 표지 그림 후보 = fig_cont(a): "컨트롤러가 배운 적 없는 낙하 문턱을 추적한다".
- 본격 서사 재배치(continuation을 동기로 승격)는 4단계 재작성에서.

## 다음: 2단계 방어 실험 3종

① adaptive move-limit 베이스라인(Table 4 행 추가; 예측 = 문턱 불변·진폭만) ② 컨트롤러 6종점 s_max 재측정(η_end vs 각 종점 자신의 2/s_max) ③ 절단 리처드슨(컷오프 {0, 0.1, 0.3, 0.5}, 컴퓨트 0).
