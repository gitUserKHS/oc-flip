# 리뷰 2라운드 대응 노트 (v3 심사, 11항목)

**날짜:** 2026-08-17 · **판정: 11/11 반영 완료 + 신규 발견 2건.**

## 정합성 스윕 (컴퓨트 0)
1. §6.2 구식 괄호(R0 0.503 비교) 삭제 → 착지 비 문장으로 개시. "within measurement resolution" 문구 채택.
2. §6.4 "never failed on this suite" → "at the nominal start (+ 방탄 아님 예고)".
3. "times seven methods" → nine; Fig 9 매트릭스 9행 재생성.
4. §5.5 회색 카운트 전역 진술 → "p=5 단조, p=3은 r2.0까지" 범위 한정.
5. **Table 4 masked 컬럼 분리** — 전 방법 μ̂ 꼬리 재계측(cc_sweep 재실행). **신규 발견: masked는 move 스케줄 전용이 아님** — fixed 0.7의 "수렴" 3런이 표준 move에서 μ̂=−1.00 조용한 한계 사이클(amp 2e-5~9e-4), ladder도 2런. fixed 0.7 실질 실패 7/11. 기준 정밀화: amp∈(1e-5, 3e-3) ∧ μ̂≤−0.9 (10⁻¹¹ 수치 바닥의 감쇠 잔류 모드는 제외 — aimd 2건이 이 케이스로 오탐 방지).
6. 초록·§6.4 시드 범위 표기 — fix0.5 시드별 최악 추출(+1.4/+1.9/+1.4%): aimd0.5(+0.7~1.6%)가 **모든 시작에서 fix0.5(+0.8~1.9%) 이하**. 체리피킹 리스크 제거.

## 신규 컴퓨트
7. **continuation 시드 복제 + vf0.4 확장** (`src/cd_seeds.py`, 156런): cant_fast fix0.5 **3/3 재현**; vf0.4에서 fix0.5 **양 스케줄 8/8 전패**(slow로도 못 구함 — 종점 문턱 < 0.5); 합계 fix0.5 12/32, fix0.7 28/32, **AIMD 0/64**. 동기가 셀 1개 → 2구성 12런으로. `data/cd_seeds.npz`.
9. **캐스케이드 직접 테스트** (`src/cg_cascade.py`): 하한 1e-3→1e-6에서도 정착 어트랙터 전부 깨끗한 주기-2 (r2/amp < 1e-6, η∈[0.9,1.6]) — **하한 클리핑은 저지 기제가 아님** (배제). η=0.9의 240반복 "비주기"는 과도(800반복에서 r2/amp=0.000). §5.2를 "bounded update가 저지, 구체 비선형성 미분리"로 정정.

## 무료 마무리
8. §6.3 요약 표(tab:cont, 8행 — vf0.4 포함) 신설.
10. §6.3 폐루프 한 줄: p=5 홀드에서 η 중앙값 0.331 × s_max 6.00 = **1.99** (양 스케줄) — 경계 정확히.
11. 결론에 "tracks that threshold when continuation moves it" 삽입.
잔가지: §5.3 절단 미니표, 42프로브 최대편차 0.154 병기(†프로브), §8 continuation 범위 명시, Fig 8 패널 구성 라벨.

## 수치 스냅샷
- 42프로브: 중앙 0.001 / 최대 0.154(† 얕은 수렴 프로브).
- fix0.5 시드별 최악 dq: +0.8(명목)/+1.4/+1.9/+1.4%; aimd0.5: +0.7/+1.4/+1.6/+1.4%.
- continuation p=5 홀드: η_med=0.331, s_max=6.00, 곱 1.99 (fast·slow 동일).
